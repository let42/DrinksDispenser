#include <iostream>
#include <string>

//Creating the guards
#ifdef _COCA_COLA_H_
#define _COCA_COLA_H_

bool theresMoneyBack( );
bool isEmpty( );
bool releaseTheProduct( );
double calculateMoneyBack( );
int checkStorage( );
void fillStorage( );


#endif
